#!/usr/bin/env python3


class Pipe:

    def __init__(self, name='', maker='', shape='', store='', buyyear=0):
        Pipe.name = name
        Pipe.maker = maker
        Pipe.shape = shape
        Pipe.store = store
        Pipe.buyyear = buyyear
